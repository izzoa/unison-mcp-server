mcp>=1.0.0,<2
google-genai>=1.19.0
openai>=1.55.2  # Minimum version for httpx 0.28.0 compatibility
pydantic>=2.0.0
python-dotenv>=1.0.0
litellm>=1.50.0  # Model metadata discovery (model_cost, get_model_info)
tiktoken>=0.7.0  # OpenAI-compatible token counting (also available via openai SDK)

# Development dependencies live in pyproject.toml [dependency-groups] dev.
# Installs consume the committed lock exports instead of these ranges:
#   runtime: pip install -r requirements.lock.txt
#   dev:     pip install -r requirements-dev.lock.txt