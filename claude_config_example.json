{
  "comment": "Example Claude Desktop configuration for Unison MCP Server",
  "comment2": "Run './run-server.sh -c' to get the exact configuration for your system",
  "comment3": "For platform-specific examples, see the examples/ directory",
  "mcpServers": {
    "unison": {
      "command": "/path/to/unison-mcp-server/.unison_venv/bin/python",
      "args": ["/path/to/unison-mcp-server/server.py"]
    }
  }
}